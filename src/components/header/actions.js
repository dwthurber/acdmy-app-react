import React, { Component } from 'react';
import { Icon } from 'semantic-ui-react';

const Actions = () => (
  <div>
    <Icon name='users' circular className="primaryIcon" />
    <Icon name='comments' circular className="primaryIcon" />
    <Icon name='grid layout' circular className="primaryIcon" />
  </div>
)

export default Actions
